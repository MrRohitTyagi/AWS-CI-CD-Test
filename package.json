{
  "name": "aws-test-express-application",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon -L index.js",
    "dbuild": "docker build -t d1 .",
    "drun": "docker run -d -p 5000:5000 -v %cd%:/app --name d1-cont d1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.13.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "pm2": "^5.3.1",
    "prisma": "^5.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
